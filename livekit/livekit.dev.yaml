port: 7880
rtc:
  udp_port: 7882
  tcp_port: 7881
  use_external_ip: false
  # Убедитесь, что IP совпадает с адресом машины. По умолчанию localhost.
  node_ip: 127.0.0.1

# devkey / secret используются локально. Не применяйте их в проде.
keys:
  devkey: secret

room:
  empty_timeout: 60

