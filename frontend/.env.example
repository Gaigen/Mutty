# Frontend env: копируй из корневого .env.example
# Полный список переменных — в корне проекта: .env.example
#
# Минимум для dev (wsUrl для LiveKit приходит в ответе токен-сервера):
# VITE_TOKEN_ENDPOINT=http://127.0.0.1:4000/api/token
# VITE_AGENT_ENDPOINT=http://127.0.0.1:5000
